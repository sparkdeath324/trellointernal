# Absolute path to the SQLite database file.
# Defaults to <project>/data/crm.db locally; the container image sets this to
# the mounted volume at /app/data/crm.db.
CRM_DB_PATH=/app/data/crm.db

# Port and bind address for the production server.
PORT=3000
HOSTNAME=0.0.0.0
